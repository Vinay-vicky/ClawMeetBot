PORT=3000

# Telegram Bot (from @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_GROUP_ID=-100your_group_id_here

# Optional: secret token for Telegram webhook verification (any random string)
# Set same value in Render env vars — bot will reject requests without it
TELEGRAM_WEBHOOK_SECRET=

# Microsoft Azure App Registration
TEAMS_APP_ID=your_azure_client_id
TEAMS_APP_PASSWORD=your_azure_client_secret
TEAMS_TENANT_ID=your_azure_tenant_id

# Optional: HMAC secret for Teams webhook verification
# Encode a random string as base64 and set it in both here and in your Teams connector config
TEAMS_WEBHOOK_SECRET=

# Outlook calendar user
OUTLOOK_USER_EMAIL=you@yourdomain.com

# Timezone (e.g. Asia/Kolkata, Asia/Dubai, UTC)
TIMEZONE=Asia/Kolkata

# Base URL for random meet links
MEET_LINK_BASE_URL=https://meet.google.com

# OpenAI (for AI meeting summaries — get from platform.openai.com)
OPENAI_API_KEY=your_openai_api_key_here

# Turso (persistent cloud SQLite — free at https://turso.tech)
# Create a DB → Settings → copy the URL and auth token
TURSO_DATABASE_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token_here

# Gemini AI (for meeting summaries — get from https://aistudio.google.com)
GEMINI_API_KEY=your_gemini_api_key_here

# Sentry.io error monitoring (optional — get DSN from sentry.io after creating a Node.js project)
SENTRY_DSN=

# Log level: error | warn | info | debug (default: info)
LOG_LEVEL=info

# Web dashboard access token — if set, /dashboard requires ?token=VALUE (optional)
DASHBOARD_TOKEN=
